'use client';

// Re-export specific components from framer-motion to avoid "export *" issues
import {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  useMotionValue,
  useSpring,
  useMotionTemplate,
  animate,
  m,
  LazyMotion,
  domAnimation,
  MotionConfig,
} from 'framer-motion';

export {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  useMotionValue,
  useSpring,
  useMotionTemplate,
  animate,
  m,
  LazyMotion,
  domAnimation,
  MotionConfig,
};
